[pytest]
# Pytest configuration for ef package

# Test discovery
testpaths = ef

# Doctest options
addopts = 
    --doctest-modules
    --ignore=imbed_refactored
    --ignore=build
    --ignore=dist
    --ignore=.eggs

# Doctest configuration
doctest_optionflags = NORMALIZE_WHITESPACE ELLIPSIS
